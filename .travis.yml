language: go

go:
  - 1.11.x

install:
  - make prepare

services:
  - postgresql

script:
  - dep ensure --vendor-only -v
  - go test -v -p 1 github.com/Peripli/service-manager/test/broker_test
  # - make precommit
  # - goveralls -coverprofile profile.cov -service=travis-ci


notifications:
  slack: openservicebrokerapi:KQo2QAC6LXeEeXPOemOXja48
